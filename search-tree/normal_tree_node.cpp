#include "normal_tree_node.h"

template<typename T>
using p2n = shared_ptr<normal_tree_node<T>>; // means pointer to tree node;

template<typename T>
shared_ptr<normal_tree_node<T>> baseTree<T>::insert(int key, T value) {
    w
}